import { Routes } from '@angular/router';
import{AirlineDetailsComponent} from './airline-details/airline-details.component'

export const routes: Routes = [{component: AirlineDetailsComponent, path: 'airlines'}];
